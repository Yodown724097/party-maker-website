# 2026-06-26

## Diwali 产品上架
- 从飞书 Base (L8CJbRxEoa2neRsvqvycAMjsnxg, tblkHyLWTAWl4yPH) 拉取 30 个 Diwali 产品
- 写入 products.json，跑 build_pages.py 生成页面
- `/diwali/` 目录 + 15 子分类页 + 30 产品详情页已生成
- **图片未完成**：飞书 bot API 月配额耗尽 (code 99991403)，需 7/1 后补下载+上传 R2
- 临时脚本：import_diwali.py, fix_diwali_images.py

## MaxHub 图片不显示问题排查 & 修复
- **根因**：R2 域名 `pub-1fd965ab66464286847edcb540254451.r2.dev` 在 MaxHub 内置浏览器上不可达（DNS/证书/网络策略）
- **修复方案**：
  1. 新增 `functions/img/[[path]].js` — Cloudflare Pages Function 图片代理，将 `/img/SKU/file` 转发到 R2
  2. 修改 app.js：`R2_PUBLIC` → 代理路径，新增 `toProxy()` + `normalizeImageUrls()` 全局转换
  3. 修复 onerror handler：不再重试同 URL，改为显示 placeholder 图标
- 产品页（build_pages.py 生成的）仍用硬编码 R2 URL，后续需要同步更新模板

## 图片 URL 全面统一到 partymaker.cn
- build_pages.py 新增 `to_proxy()` + `proxy_images()`，所有 HTML 输出中的图片 URL 转为 `https://www.partymaker.cn/img/SKU/file.webp`
- 覆盖范围：产品详情页（主图+关联产品缩略图）、分类页、博客页、sitemap
- products-public.json 同步转换
- index.html 嵌入数据修复（同时修了一个 `/app.js` vs `app.js` 路径匹配的历史 bug）
- app.js 保留 `normalizeImageUrls()` 做安全兜底
- 全站 996 产品页 + 49 分类页 + 16 博客页完全无 R2 原始域名引用
