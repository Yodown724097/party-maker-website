# 2026-07-13

## Weekly Sitemap Refresh (Automation)
- **Sitemap**: Regenerated via build_pages.py — 902 URLs, 0 product changes (dates preserved by smart mode) ✓
- **Robots.txt**: Unchanged, sitemap reference present ✓
- **Google Ping**: HTTP 404 (endpoint deprecated) — expected
- **Bing Ping**: HTTP 410 Gone (endpoint deprecated) — expected
- **Git**: Committed `.build_cache.json`, pushed to main ✓
- **Note**: Google/Bing discover sitemap via robots.txt + Search Console; ping endpoints no longer functional.

## Sitemap Cron Migrated to VPS
- Moved weekly sitemap refresh from local WorkBuddy automation → VPS crontab
- VPS path: `/root/party-maker-website/` (shallow clone from GitHub)
- Script: `/root/scripts/sitemap_refresh.sh` (git pull → build → ping → git push)
- Cron: every Monday 8:00 AM Beijing (`0 8 * * 1`)
- Local automation `automation-1781581832976` paused
