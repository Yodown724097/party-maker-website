# 2026-06-08

## SEO 修复 + 重新部署
- 批量检测了 sitemap 中 1000 个 URL 的 HTTP 状态码：997 个 200，3 个连接超时（暂态），无 5xx
  - Google Search Console 报告的 4 个 5xx 可能是旧版或暂态错误
- 修复了 `build_pages.py` 的 fp-render JS 语法错误（`+'</a>'+'<div` 缺 `+` 号）
- 在 `robots.txt` 中新增 `Disallow: /*?p=*` 阻止 `?p=SKU` 参数页面的重复内容被索引
- 重新运行 build_pages.py 生成 966 产品页 + 33 分类页 + 更新 sitemap.xml
- 提交到 GitHub 并推送到 Cloudflare Pages，部署已生效

## 诊断发现
- `/?p=SKU` 与 `/product/SKU/` 内容重复 → 通过 robots.txt 阻止参数 URL 索引
- 产品页 on-page SEO 结构正常（title / meta / canonical / h1 / robots）

## 待续
- P1: 丰富产品页内容（加规格表、使用场景）
